# cart_recovery_portfolio
An n8n workflow that automatically detects abandoned shopping carts, waits for a predefined period, sends personalized recovery emails, and updates PostgreSQL to prevent duplicate reminders.
